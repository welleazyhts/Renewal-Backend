from django.apps import AppConfig

class RenewalTimelineConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.renewal_timeline"
    verbose_name = "Renewal Timeline"
